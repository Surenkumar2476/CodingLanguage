#include<iostream>                         // incase you are using gcc compiler use iostream.h or it is a latest version put only iostream

using namespace std;                       // using namespace std --- std holds the cout and cin variable if not declared 
                                           // the using namespace std, cout will be error which is not assigned cout
int main()
{
    cout<<"Hello World.....!";              // << -- insertion operator
    return 0; 
}
